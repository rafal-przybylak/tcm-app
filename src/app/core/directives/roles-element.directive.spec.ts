import { RolesElementDirective } from './roles-element.directive';

describe('RolesElementDirective', () => {
  it('should create an instance', () => {
    const directive = new RolesElementDirective();
    expect(directive).toBeTruthy();
  });
});
