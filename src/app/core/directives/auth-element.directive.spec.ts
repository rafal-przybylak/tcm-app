import { AuthElementDirective } from './auth-element.directive';

describe('AuthElementDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthElementDirective();
    expect(directive).toBeTruthy();
  });
});
